# Test Bootstrap Placeholder

Future home of the `test` environment state/bootstrap root.

Promote from `stacks/dev/platform-v2/bootstrap` when the test environment gets
real subscription ids and backend configuration.
