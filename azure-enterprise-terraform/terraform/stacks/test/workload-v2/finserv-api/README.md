# Test FinServ API Placeholder

Future home of the `test` workload deployment for `finserv-api`.

Expected subscription role:

- nonprod workload subscription: `<replace-with-test-workload-subscription-id>`

Promote from `stacks/dev/workload-v2/finserv-api` when test shared-platform
state is available.
