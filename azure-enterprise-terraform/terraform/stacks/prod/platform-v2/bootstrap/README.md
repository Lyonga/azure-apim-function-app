# Prod Bootstrap Placeholder

Future home of the `prod` environment state/bootstrap root.

Promote from `stacks/dev/platform-v2/bootstrap` when the production environment
gets real subscription ids and backend configuration.
