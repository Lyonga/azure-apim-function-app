# Prod Environment Placeholder

This environment is a placeholder scaffold that mirrors the active `dev` v2
layout for review purposes.

Before making `prod` deployable, replace placeholders with real values:

- platform subscription id: `<replace-with-prod-platform-subscription-id>`
- workload subscription id: `<replace-with-prod-workload-subscription-id>`
- backend keys under `stacks/prod/...`
- production naming, tagging, and approval controls

Do not treat this placeholder as an active CI target until that replacement is
done.
